---
# English portfolio list — chains to portfolio-list so the rendering is shared,
# but a different layout key keeps it out of the theme's nav (which only shows
# pages whose layout matches the button key `portfolio-list`).
layout: portfolio-list
---
{{ content }}
