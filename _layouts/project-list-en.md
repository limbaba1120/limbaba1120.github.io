---
# English project list — chains to project-list so the rendering is shared,
# but a different layout key keeps it out of the theme's nav (which only shows
# pages whose layout matches the button key `project-list`).
layout: project-list
---
{{ content }}
