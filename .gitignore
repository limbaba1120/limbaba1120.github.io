# this will handle to macos hidden folders
# ignore all starts with .* except .git and so on
.*
!.git*
!.editorconfig

# jekyll Cache
_site
.sass-cache
.jekyll-metadata
Gemfile.lock

# OS generated
ehthumbs.db
Thumbs.db

# diff files
*.bak

# updating Gemfile automatically using github actions
vendor

# Node tooling for PDF export (`scripts/export-pdf.mjs`).
node_modules
out

