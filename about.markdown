---
# Legacy `/about/` URL — the real About page lives at `/tabs/about/` (theme convention).
# Uses jekyll-redirect-from (already in plugins) so old links keep working.
permalink: /about/
redirect_to: /tabs/about/
sitemap: false
---
